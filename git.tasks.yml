---
version: '3'

vars:

tasks:
  pull:
    desc: "Pull and rebase"
    cmds:
      - git pull --rebase

  submodule:update:
    desc: "Update submodules"
    cmds:
      - git submodule update --init --recursive
